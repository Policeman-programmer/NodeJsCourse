/// <reference types="node" />
import * as stream from "stream";
import * as ts from "typescript";
import * as input from "./input";
import * as project from "./project";
import * as reporter from "./reporter";
export declare class Output {
    public project: project.ProjectInfo;
    public result: reporter.CompilationResult;
    public streamFull: stream.Readable;
    public streamJs: stream.Readable;
    public streamDts: stream.Readable;
    private pendingIO;
    private writeJsAsync;
    private writeDtsAsync;
    private applySourceMap;
    private pipeRejection;
    private mightFinish;
    private getError;
    constructor(_project: project.ProjectInfo, streamFull: stream.Readable, streamJs: stream.Readable, streamDts: stream.Readable);
    public writeJs(base: string, fileName: string, content: string, sourceMapContent: string, cwd: string, original: input.File): void;
    public writeDts(base: string, fileName: string, content: string, declarationMapContent: string, cwd: string, original: input.File): void;
    public finish(result: reporter.CompilationResult): void;
    public diagnostic(info: ts.Diagnostic): void;
    public error(error: reporter.TypeScriptError): void;
}
